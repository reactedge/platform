import {ValidationIssue} from "../validator.ts";

export function validateJson(
    contract: unknown,
    cdn: string
): ValidationIssue[] {

    const issues: ValidationIssue[] = [];

    return issues;
}